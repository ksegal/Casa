json.extract! user, :id, :username, :password_digest, :is_admin, :first_name, :last_name, :email, :phone, :fb_link, :picture, :gender, :is_owner, :description, :created_at, :updated_at
json.url user_url(user, format: :json)